#include "myheader.h"
#include <stdio.h>
#define a 100
//define a 100;

int i, r, c;
int 
A[
    100];
//wahthappen
int addi(int     a, int b);
int fact1   (int a);
int 
fact2   (int a);

int addi (int a, int b){
    return 1+ 2;
}

int fact1 (int a) 

{
    int 
    
    
    B[1] = {1};
    int i =1,    
        sum = 1;while (i <= a) {
        sum = sum* i;
        i = i+ 1 - A[2+5];
    }
            return i;
}

int fact2 (int a) {
    int sum = 0;
    if 
    
    (a > 0)
        if (b > 0)
            c = a + b;
    for 
    
    (int i = 0; i< a; i = i+1)sum = sum*i;
    return sum;
}

int isodd(int a) {
    if (a 
    %2 == 1) {
        if(a > 0)
            return 1;
        else 
            return 0;
    }
    return 0;
}int               main () 
{
    int a =7, b = 04;
    int c = 0x23;
    c = c%3- a *add(a+1,b)+fact(a)+0x111;
    if(isodd(c)) return 1;
    else return 0;
}