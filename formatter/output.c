//define a 100;
//wahthappen
//test

#include "myheader.h"
#include <stdio.h>
#define a 100

int i, r, c;

int A[100];

int addi(int a);

int fact1(int a);

int fact2(int a);

int addi(int a) {
	return 1 + 2;
}

int fact1(int a) {
	int B[1] = {1}
	int i, 1;

	while(i <= a) {
		sum = sum * i;
		i = i + 1 - A[2 + 5];
	}
	return i;
}

int fact2(int a) {
	int sum;
	if(a > 0) 
		if(b > 0) 
			c = a + b;
	for(int i;
; i < a; i = i + 1) 
		sum = sum * i;
	return sum;
}

int isodd(int a) {
	if(a % 2 == 1) {
		if(a > 0) 
			return 1;
		else 
			return 0;
	}
	return 0;
}

int main() {
	int a, 7;

	int c;
	c = c % 3 - a * add(a + 1, b) + fact(a) + 273;
	if(isodd(c)) 
		return 1;
	else 
		return 0;
}

